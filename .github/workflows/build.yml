name: Rust

on:
  push:
    branches: [ * ]
  pull_request:
    branches: [ * ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    runs-on: ubuntu-latest


    steps:

    - name: Build image
      run: ./_build.sh
